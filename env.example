# ===========================================
# CONFIGURAÇÃO DO SUPABASE
# ===========================================
# URL do seu projeto Supabase (Settings > API > Project URL)
SUPABASE_URL=https://your-project.supabase.co

# Chave anônima do Supabase (Settings > API > anon public)
SUPABASE_KEY=your_supabase_anon_key_here

# Chave de service role do Supabase (Settings > API > service_role secret)
# ATENÇÃO: Esta chave tem acesso total ao banco, mantenha segura!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ===========================================
# CONFIGURAÇÃO JWT
# ===========================================
# Chave secreta para assinar tokens JWT (mínimo 32 caracteres)
# Gere uma chave segura: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_secure_secret_key_here_minimum_32_characters

# Algoritmo de criptografia JWT
JWT_ALG=HS256

# Tempo de expiração do token em minutos (60 = 1 hora)
JWT_EXPIRES_MIN=60

# ===========================================
# CONFIGURAÇÃO DE AMBIENTE
# ===========================================
# Ambiente da aplicação (dev, prod, test)
ENV=dev

# ===========================================
# CONFIGURAÇÃO DE SEGURANÇA (Opcional)
# ===========================================
# Origens permitidas para CORS (separadas por vírgula)
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200,https://meuapp.com

# Habilitar proteção CSRF (true/false)
# CSRF_PROTECTION_ENABLED=true

# Usar cookies seguros (true para HTTPS, false para desenvolvimento local)
# SECURE_COOKIES=false
